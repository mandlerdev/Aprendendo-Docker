{
  "name": "aprendendo-docker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:migrate": "sequelize-cli db:migrate --migrations-path src/database/migrations --config src/config/database.js",
    "db:migrate:undo": "sequelize-cli db:migrate:undo --migrations-path src/database/migrations --config src/config/database.js",
    "migration:generate": "sequelize-cli migration:generate --migrations-path src/database/migrations"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.23.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.8",
    "sequelize-cli": "^6.6.5"
  }
}
